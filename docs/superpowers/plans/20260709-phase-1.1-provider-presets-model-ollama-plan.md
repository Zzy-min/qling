# Plan: Phase 1.1 Provider Presets / Model / Ollama

**关联 Spec**: `docs/superpowers/specs/20260709-phase-1.1-provider-presets-model-ollama-spec.md`

## 实施顺序

1. 新建 `src/providers/presets.ts`（预设 + 解析）  
2. 改 `cli/setup.ts` 使用共享预设  
3. 扩展 `AgentLoop` LLM session API + 本地空 key  
4. 增强 `/model`  
5. `doctor` 增加 ollama 探测  
6. 补单测 → `npm run build && npm test`

## 风险

- doctor 多一个 warn 会改变 `summary.pass` 下限：更新 doctor 测试期望  
- setup 序号与 i18n 文案保持 1–11 对齐  
