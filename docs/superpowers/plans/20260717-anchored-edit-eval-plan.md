# 锚定编辑收益门禁实施计划

1. 建立20个确定性 fixture，覆盖原样、整体位移、宽上下文失效、规范化空白、目标被外部修改和重复锚点。
2. 对同一陈旧快照分别执行普通 `patch` 与实验性 `patch_anchored`，只把最终文件完全符合预期计为成功。
3. 对安全 fixture 计算成功率百分点差；要求锚定编辑至少提升15个百分点。
4. 对全部 fixture 检查错误写入和不安全接受；任一非零即失败。
5. 将评测加入 `npm run ci:check`，保持纯本机、无模型、无网络、临时目录自动清理。
