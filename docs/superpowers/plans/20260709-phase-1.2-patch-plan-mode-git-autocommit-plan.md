# Plan: Phase 1.2 Patch / Plan Mode / Git Auto-Commit

**关联 Spec**: `docs/superpowers/specs/20260709-phase-1.2-patch-plan-mode-git-autocommit-spec.md`

## 顺序

1. `src/git/auto-commit.ts`  
2. `src/tools/patch.ts` 增强  
3. `HookManager` plan mode  
4. `AgentLoop` plan + post-commit  
5. `/plan` 命令  
6. 单测 + `npm run build && npm test`

## 验证

- patch dry_run 不改文件  
- plan 下 write 权限拒绝  
- autoCommit=off 为默认无副作用  
