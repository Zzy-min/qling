# 轻灵运行效率与副作用门禁实施计划

1. 为 prompt 去重、运行效率守卫、压缩预算和副作用台账补 RED 测试。
2. 新增纯运行效率守卫，归一化失败指纹并维护失败、压缩和文件变更状态。
3. 扩展工具编排结果，返回失败与文件变更观察，不改变既有工具结果消息。
4. 在 inner loop 接入熔断、压缩预算和副作用合成消息。
5. 去除强制规则三重注入，保留一份完整 system 规则和一条短引用。
6. 加强基座文案：优先内联只读诊断、限制同类回退、交付披露完整副作用台账。
7. 运行定向测试、完整门禁和基线对比，记录剩余边界。
